{
  "options": {
    "step": {
      "init": {
        "data": {
          "subscribe_logs": "Subscribe to logs from device"
        },
        "data_description": {
          "subscribe_logs": "Stream log messages from the ESPHome device into the Home Assistant logs panel."
        }
      }
    }
  },
  "config": {
    "abort": {
      "already_configured": "This device is already configured.",
      "cannot_connect": "Cannot connect to the CoAP server."
    },
    "error": {
      "cannot_connect": "Cannot connect to the CoAP server. Check the host and port.",
      "invalid_hex": "Must be a valid hex string (e.g. 0102abcd).",
      "oscore_field_required": "Required when Master Secret is set.",
      "oscore_ids_same": "Sender ID and Recipient ID must be different."
    },
    "step": {
      "oscore": {
        "data": {
          "id_context": "ID Context",
          "master_salt": "Master Salt",
          "master_secret": "Master Secret",
          "recipient_id": "Recipient ID",
          "sender_id": "Sender ID"
        },
        "data_description": {
          "id_context": "Hex string — same as id_context in your ESPHome config (optional).",
          "master_salt": "Hex string — same as master_salt in your ESPHome config (optional).",
          "master_secret": "Hex string — same as master_secret in your ESPHome config.",
          "recipient_id": "Hex string — same as recipient_id in your ESPHome config (your client ID).",
          "sender_id": "Hex string — same as sender_id in your ESPHome config (the device's sender ID)."
        },
        "description": "Enter the OSCORE credentials from your ESPHome coap_server config.",
        "title": "OSCORE Security"
      },
      "user": {
        "data": {
          "host": "Host",
          "port": "Port"
        },
        "data_description": {
          "host": "Hostname or IP address of the CoAP server",
          "port": "CoAP UDP port (default 5683)"
        },
        "title": "Connect to ESPHome CoAP Device"
      },
      "zeroconf_confirm": {
        "description": "Do you want to add **{name}**?",
        "title": "Discovered CoAP Device"
      }
    }
  }
}
